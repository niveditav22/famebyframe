<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300..700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="public/app.css" rel="stylesheet">
</head>
<body>
  <body>
    <%- include('./partials/navbar') %>
    <div class="container feed-title"><p class="h2"><strong>Home</strong></p></div>
    <div class="container feed-body  mt-4">
      <% if (images && images.length > 0) { %>
        <div class="row">
          <% images.forEach(function(image, index) { %>
            <div class="col-md-4 mb-4">
              <img src="/uploads/<%= image.image_url %>" alt="<%= image.image__alt_text %>" class="img-fluid" />
              <br>
              <div class="below-pic pt-1">
                <p class="caption">
                  
                  <strong class="username"><%= image.user_username %>:</strong></a>
                  <span class="caption-text"><%= image.image_caption %></span>
                  <br>
                  <span class="postdate-text"><%= image.image_post_date %></span>
                </p>
                
              </div>
            </div>
            <% if ((index + 1) % 3 === 0 && index + 1 !== images.length) { %>
              </div><div class="row">
            <% } %>
          <% }); %>
        </div>
      <% } else { %>
        <p>No images to display.</p>
      <% } %>
    </div>
  </body>

</html>
