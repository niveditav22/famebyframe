<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/app.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300..700&display=swap" rel="stylesheet">
    <title>User Profile: <%= user.user_name %></title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
</head>
<body>
    <%- include('./partials/navbar') %>
    <div class="container user-title"><p class="h2 greeting-name"><strong><%= user.user_name %></strong></p></div>
    
    <div class="container user-body  mt-4">
        <div class="container post-text"><strong>Your posts:</strong></div>  
            
        <div class="container userPage-body  mt-4">
          <% if (images && images.length > 0) { %>
            <div class="row">
              <% images.forEach(function(image, index) { %>
                <div class="col-md-4 mb-4">
                  <img src="/uploads/<%= image.image_url %>" alt="<%= image.image__alt_text %>" class="img-fluid" />
                  <br>
                  <div class="below-pic pt-1">
                    <p class="caption">
                      
                      <strong class="username"><%= user.user_username %>:</strong></a>
                      <span class="caption-text"><%= image.image_caption %></span>
                      <br>
                      <span class="postdate-text"><%= image.image_post_date %></span>
                    </p>
                    
                  </div>
                </div>
                <% if ((index + 1) % 3 === 0 && index + 1 !== images.length) { %>
                  </div><div class="row">
                <% } %>
              <% }); %>
            </div>
          <% } else { %>
            <p>You don't have any posts yet. Click the upload icon to create your first post!</p>
          <% } %>
        </div>
          </body>


    <!--<% if (images && images.length > 0) { %>
            <% images.forEach(function(image) { %>
                
                <img src="/uploads/<%= image.image_url %>" alt="<%= image.image_alt_text %>" />
                <p><%= image.image_caption %></p>
            <% }); %>
        <% } else { %>
            <p>No posts to display.</p>
        <% } %>-->
        
      </div>
</body>
</html>